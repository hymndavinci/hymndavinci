name: Metrics
on:
  workflow_dispatch:

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          user: YOUR_USERNAME
          token: ${{ secrets.METRICS_TOKEN }}
          plugin_languages: yes
